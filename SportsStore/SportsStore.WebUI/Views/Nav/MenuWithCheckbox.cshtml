@model IEnumerable<string>
@*@Html.ActionLink("Home", "List", "Product")
@foreach (var link in Model)
{
    @Html.RouteLink(link, new
{
    controller = "Product",
    action = "List",
    category = link,
    page = 1
},
new
{
    @class = link == ViewBag.SelectedCategory ? "selected" : null
})
}*@
<button class="btn btn-info" data-toggle="collapse" data-target="#hide-me">Выбор категории</button>
<div id="hide-me" class="collapse in">
   @using (Html.BeginForm("ListWithOtherPagePost", "Product"))
   {
    <br />
       foreach (var item in Model)
       {
        @Html.CheckBox(item, (Request.Form[item] ?? string.Empty).Contains("true"));
        @Html.DisplayFor(i => item);
        <br />
       }
    <input type="submit" value="Отправить" />
   }
</div>

